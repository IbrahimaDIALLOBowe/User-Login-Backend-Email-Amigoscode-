package com.backend.User.Login.Backend.Email.appuser;

// This Enum is a special "class" that represents a group of constants (unchangeable variables, like final variables). To create an enum , use the enum keyword

public enum AppUserRole {
    USER,
    ADMIN
}
