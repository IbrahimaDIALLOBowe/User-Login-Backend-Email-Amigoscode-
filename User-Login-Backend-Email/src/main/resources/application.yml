server:
  error:
    include-message: always
    include-binding-errors: always

spring:
  datasource:
    password: Hawaalphadio1&
    url: jdbc:postgresql://localhost:3306/registration
    username: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgresSQLDialect
        format_sql: true
    show-sql: true